/// ⚠️ TEMPLATE FILE - Copy this to secrets.dart and fill in your values ⚠️
/// 
/// This is a template file showing the structure of secrets.dart
/// Copy this file to secrets.dart and replace placeholder values with your actual API keys.
/// 
/// secrets.dart is gitignored and will not be committed to version control.

class Secrets {
  // Firebase API Keys (Client-side keys - restricted by package/bundle ID)
  static const iosApiKey = 'YOUR_IOS_FIREBASE_API_KEY_HERE';
  static const androidApiKey = 'YOUR_ANDROID_FIREBASE_API_KEY_HERE';
  
  // Vertex AI Configuration
  // ⚠️ WARNING: Never commit API keys or tokens to git!
  
  // Option 1: OAuth2 Access Token (for Vertex AI API - recommended)
  // Get token by running: gcloud auth print-access-token
  // Token expires after 1 hour - refresh as needed
  // Set to null to use Generative AI API with API key instead
  static const String? vertexAiAccessToken = null; // OAuth2 access token (get via: gcloud auth print-access-token)
  
  // Option 2: API Key (for Generative AI API - fallback)
  // For testing: Add your API key here temporarily
  // For production: Use Cloud Functions instead
  static const String? vertexAiApiKey = 'YOUR_VERTEX_AI_API_KEY_HERE'; // Fallback API key (if no access token)
}
